The scripts to run to allow alembic to convert one version of the models to another in the underlying database.

TODO:Re-evaluate Migration Process with version 1 release.

Known Issues: When using SQLite, alter commands used can only add columns or change column names.

When Changes are made to the model the following steps are requiered for now.
Remove all files from the "versions" folder and reinitialize the database with Flask DB Init, and Flask DB upgrade.
