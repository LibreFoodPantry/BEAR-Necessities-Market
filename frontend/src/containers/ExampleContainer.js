import React from 'react';

const ExamplePage = () => {

    const styles = {
        example: {
            fontSize: 25
        }
    };

    return (
        <div style={styles.example}>
        Example feature container
        </div>
    );
};

export default ExamplePage;